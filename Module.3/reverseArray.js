function reverseArray(arr) {
    return arr.reverse();
}

const originalArray = ['Mango', 'Orange', 'Banana', 'Apple', 'Pineapple'];

const reversedArray = reverseArray(originalArray);

console.log(reversedArray); 

